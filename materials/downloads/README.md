# Workshop downloads

The workshop zip archives in this directory are generated from
`materials/starter` and `materials/complete`. The no-code competency assessment
archive is generated from `materials/assessment-starter`.

Render the relevant project after changing a source tree, then rebuild its
archive before distribution. Render the book again so the updated downloads are
copied into `_site`.
